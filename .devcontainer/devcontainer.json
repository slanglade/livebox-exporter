{
    "name": "Livebox Exporter Dev",
    "image": "mcr.microsoft.com/devcontainers/go:1",
    "mounts": [
        "source=${localEnv:HOME}/.gitconfig,target=/home/${containerUser}/.gitconfig,type=bind,consistency=cached",
        "source=${localEnv:HOME}/.ssh,target=/home/${containerUser}/.ssh,type=bind,consistency=cached"
    ],
    "customizations": {
        "vscode": {
            "extensions": ["golang.go"]
        }
    }
}
